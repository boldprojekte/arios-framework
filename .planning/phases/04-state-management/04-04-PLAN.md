---
phase: 04-state-management
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/arios-cli/templates/.claude/commands/arios/execute.md
  - packages/arios-cli/templates/.claude/commands/arios/ideate.md
  - packages/arios-cli/templates/.claude/commands/arios/orchestrate.md
  - packages/arios-cli/templates/.claude/commands/arios/plan.md
  - packages/arios-cli/templates/.claude/commands/arios/start.md
  - packages/arios-cli/templates/.claude/commands/arios/update.md
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "No slash command file contains .arios/ path references"
    - "All state file references use .planning/STATE.md path"
    - "All config references use .planning/config.json path"
    - "All roadmap references use .planning/ base path"
  artifacts:
    - path: "packages/arios-cli/templates/.claude/commands/arios/execute.md"
      provides: "Execution workflow command"
      contains: ".planning/"
    - path: "packages/arios-cli/templates/.claude/commands/arios/ideate.md"
      provides: "Ideation workflow command"
      contains: ".planning/"
    - path: "packages/arios-cli/templates/.claude/commands/arios/orchestrate.md"
      provides: "Orchestrator coordination command"
      contains: ".planning/"
    - path: "packages/arios-cli/templates/.claude/commands/arios/plan.md"
      provides: "Planning workflow command"
      contains: ".planning/"
    - path: "packages/arios-cli/templates/.claude/commands/arios/start.md"
      provides: "Setup completion command"
      contains: ".planning/"
    - path: "packages/arios-cli/templates/.claude/commands/arios/update.md"
      provides: "Update command"
      contains: ".planning/"
  key_links:
    - from: "All 6 slash commands"
      to: ".planning/ directory"
      via: "Path references in Context and Workflow sections"
      pattern: "@?\\.planning/"
---

<objective>
Complete path migration from .arios/ to .planning/ in remaining 6 slash command files.

Purpose: Plan 04-02 partially migrated paths (arios.md and status.md completed). Six other slash command files still reference .arios/ paths, creating inconsistency across commands.

Output: All slash commands consistently reference .planning/ paths.
</objective>

<execution_context>
@/Users/j.franke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/j.franke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-state-management/04-VERIFICATION.md

Reference for correct path patterns (already migrated):
@packages/arios-cli/templates/.claude/commands/arios/arios.md
@packages/arios-cli/templates/.claude/commands/arios/status.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate execute.md paths</name>
  <files>packages/arios-cli/templates/.claude/commands/arios/execute.md</files>
  <action>
Replace all .arios/ references with .planning/ in execute.md.

Specific replacements:
- `.arios/STATE.md` -> `.planning/STATE.md`
- `.arios/config.json` -> `.planning/config.json`
- `.arios/roadmaps/` -> `.planning/roadmaps/`
- `ls .arios/` -> `ls .planning/`
- `@.arios/` -> `@.planning/`

Keep the same structure, just update paths.

Note: roadmap paths may change structure in future phases but for now maintain parallel structure.
  </action>
  <verify>
Run: `grep -c "\.arios/" packages/arios-cli/templates/.claude/commands/arios/execute.md`
Expected: 0 (no remaining .arios/ references)
  </verify>
  <done>
execute.md has no .arios/ references, all paths use .planning/
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate ideate.md paths</name>
  <files>packages/arios-cli/templates/.claude/commands/arios/ideate.md</files>
  <action>
Replace all .arios/ references with .planning/ in ideate.md.

Specific replacements:
- `.arios/STATE.md` -> `.planning/STATE.md`
- `.arios/config.json` -> `.planning/config.json`
- `ls .arios/` -> `ls .planning/`
- `@.arios/` -> `@.planning/`
  </action>
  <verify>
Run: `grep -c "\.arios/" packages/arios-cli/templates/.claude/commands/arios/ideate.md`
Expected: 0 (no remaining .arios/ references)
  </verify>
  <done>
ideate.md has no .arios/ references, all paths use .planning/
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate orchestrate.md paths</name>
  <files>packages/arios-cli/templates/.claude/commands/arios/orchestrate.md</files>
  <action>
Replace all .arios/ references with .planning/ in orchestrate.md.

Specific replacements:
- `.arios/STATE.md` -> `.planning/STATE.md`
- `.arios/config.json` -> `.planning/config.json`
- `.arios/roadmaps/` -> `.planning/roadmaps/`
- `@.arios/` -> `@.planning/`

The orchestrator has the most path references due to spawn patterns - update all instances.
  </action>
  <verify>
Run: `grep -c "\.arios/" packages/arios-cli/templates/.claude/commands/arios/orchestrate.md`
Expected: 0 (no remaining .arios/ references)
  </verify>
  <done>
orchestrate.md has no .arios/ references, all paths use .planning/
  </done>
</task>

<task type="auto">
  <name>Task 4: Migrate plan.md paths</name>
  <files>packages/arios-cli/templates/.claude/commands/arios/plan.md</files>
  <action>
Replace all .arios/ references with .planning/ in plan.md.

Specific replacements:
- `.arios/STATE.md` -> `.planning/STATE.md`
- `.arios/config.json` -> `.planning/config.json`
- `.arios/roadmaps/` -> `.planning/roadmaps/`
- `ls .arios/` -> `ls .planning/`
- `@.arios/` -> `@.planning/`
  </action>
  <verify>
Run: `grep -c "\.arios/" packages/arios-cli/templates/.claude/commands/arios/plan.md`
Expected: 0 (no remaining .arios/ references)
  </verify>
  <done>
plan.md has no .arios/ references, all paths use .planning/
  </done>
</task>

<task type="auto">
  <name>Task 5: Migrate start.md paths</name>
  <files>packages/arios-cli/templates/.claude/commands/arios/start.md</files>
  <action>
Replace all .arios/ references with .planning/ in start.md.

Specific replacements:
- `.arios/config.json` -> `.planning/config.json`
- `@.arios/` -> `@.planning/`
  </action>
  <verify>
Run: `grep -c "\.arios/" packages/arios-cli/templates/.claude/commands/arios/start.md`
Expected: 0 (no remaining .arios/ references)
  </verify>
  <done>
start.md has no .arios/ references, all paths use .planning/
  </done>
</task>

<task type="auto">
  <name>Task 6: Migrate update.md paths</name>
  <files>packages/arios-cli/templates/.claude/commands/arios/update.md</files>
  <action>
Replace all .arios/ references with .planning/ in update.md.

Specific replacements:
- `.arios/config.json` -> `.planning/config.json`
- `@.arios/` -> `@.planning/`
  </action>
  <verify>
Run: `grep -c "\.arios/" packages/arios-cli/templates/.claude/commands/arios/update.md`
Expected: 0 (no remaining .arios/ references)
  </verify>
  <done>
update.md has no .arios/ references, all paths use .planning/
  </done>
</task>

</tasks>

<verification>
Run comprehensive check for any remaining .arios/ references:
```bash
grep -r "\.arios/" packages/arios-cli/templates/.claude/commands/arios/
```
Expected: Empty output (no matches)

Verify .planning/ references exist:
```bash
grep -l "\.planning/" packages/arios-cli/templates/.claude/commands/arios/*.md | wc -l
```
Expected: 8 (all slash command files)
</verification>

<success_criteria>
- Zero .arios/ references in any slash command file
- All 6 files updated with .planning/ paths
- Consistent path structure across all ARIOS commands
- No functional changes beyond path updates
</success_criteria>

<output>
After completion, create `.planning/phases/04-state-management/04-04-SUMMARY.md`
</output>
