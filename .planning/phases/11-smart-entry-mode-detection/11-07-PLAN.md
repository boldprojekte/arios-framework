---
phase: 11-smart-entry-mode-detection
plan: 07
type: execute
wave: 2
depends_on: [11-06]
files_modified:
  - packages/arios-cli/templates/.claude/commands/arios/arios.md
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "User can discover /arios:change-mode command from help"
    - "arios.md documents all available mode commands"
    - "Command description matches actual change-mode.md behavior"
  artifacts:
    - path: "packages/arios-cli/templates/.claude/commands/arios/arios.md"
      provides: "Documentation of /arios:change-mode command"
      contains: "change-mode"
  key_links:
    - from: "arios.md Available Commands section"
      to: "change-mode.md"
      via: "Command listing"
      pattern: "change-mode.*Override"
---

<objective>
Document /arios:change-mode in arios.md available commands section

Purpose: MODE-03 requirement states users can override mode, which is implemented via /arios:change-mode (created in plan 11-06). However, users won't know this command exists unless it's documented in the main arios.md help/commands section. This plan completes the MODE-03 integration.

Output: Updated arios.md with /arios:change-mode listed in available commands.
</objective>

<execution_context>
@/Users/j.franke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/j.franke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-smart-entry-mode-detection/11-CONTEXT.md
@packages/arios-cli/templates/.claude/commands/arios/arios.md
@packages/arios-cli/templates/.claude/commands/arios/change-mode.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add /arios:change-mode to Available Commands section</name>
  <files>packages/arios-cli/templates/.claude/commands/arios/arios.md</files>
  <action>
In arios.md, find the "## Available Commands" section (or equivalent command listing).

Add `/arios:change-mode` to the command list with appropriate description:

```markdown
| `/arios:change-mode` | Override detected mode (switch between Feature-Mode and Project-Mode) |
```

OR if commands are listed in a different format, match that format:

```markdown
- `/arios:change-mode` - Override detected mode (switch between Feature-Mode and Project-Mode)
```

Place the command logically near other mode-related commands (like `/arios:feature` and `/arios:project`).
  </action>
  <verify>
    1. Read arios.md and confirm /arios:change-mode is listed in available commands
    2. Run: `grep "change-mode" packages/arios-cli/templates/.claude/commands/arios/arios.md` to confirm presence
  </verify>
  <done>/arios:change-mode appears in arios.md available commands with clear description</done>
</task>

<task type="auto">
  <name>Task 2: Add mode override hint in Mode Detection section</name>
  <files>packages/arios-cli/templates/.claude/commands/arios/arios.md</files>
  <action>
In arios.md, find the Mode Detection section where modes are explained.

Add a note about mode override capability:

```markdown
**Wrong mode detected?** Run `/arios:change-mode` to switch modes at any time.
```

This ensures users know they can correct the mode if auto-detection guessed wrong.
  </action>
  <verify>Read arios.md Mode Detection section and confirm override hint is present</verify>
  <done>Mode Detection section includes hint about /arios:change-mode for override</done>
</task>

</tasks>

<verification>
- [ ] arios.md lists /arios:change-mode in available commands
- [ ] Command description accurately reflects mode switching capability
- [ ] Mode Detection section mentions override option
- [ ] grep "change-mode" arios.md returns matches
</verification>

<success_criteria>
- Users can discover /arios:change-mode from arios.md help
- MODE-03 requirement (user can override mode) is fully integrated
- Command placement is logical within arios.md structure
</success_criteria>

<output>
After completion, create `.planning/phases/11-smart-entry-mode-detection/11-07-SUMMARY.md`
</output>
