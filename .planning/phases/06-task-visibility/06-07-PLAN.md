---
phase: 06-task-visibility
plan: 07
type: execute
wave: 5
depends_on: ["06-06"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "User confirms dashboard loads and displays task data"
    - "User confirms real-time updates work when files change"
    - "User confirms view switching works (Kanban/List/Roadmap)"
    - "User confirms task detail modal works"
---

<objective>
Verify the complete dashboard functionality with the user.

Purpose: Human verification ensures the dashboard meets the user's expectations for visual design, usability, and real-time behavior that automated tests cannot fully validate.
Output: User approval or specific feedback for improvements.
</objective>

<execution_context>
@/Users/j.franke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/j.franke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-task-visibility/06-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete ARIOS dashboard with real-time task visibility</what-built>
  <how-to-verify>
1. Start the dashboard:
   ```
   cd packages/arios-cli && npm run build
   cd packages/arios-dashboard && npm install && npm run build
   cd /path/to/project && npx arios dashboard
   ```

2. Verify browser opens to http://localhost:3456

3. Check Tasks view (Kanban):
   - Tasks appear in correct columns (Pending, In Progress, Complete)
   - Task cards show ID, name, wave, dependencies
   - Click a task card to open detail modal
   - Modal shows full task information

4. Check view toggle:
   - Click "List" button
   - Verify tasks appear in table format
   - Click back to "Kanban"

5. Check dependency lines:
   - In Kanban view, verify lines connect dependent tasks
   - Resize window, verify lines update

6. Check Roadmap tab:
   - Click "Roadmap" in sidebar
   - Verify phases display with progress bars
   - Verify roadmap content renders

7. Test real-time updates:
   - Open a terminal
   - Touch a file in .planning (e.g., `touch .planning/STATE.md`)
   - Verify dashboard updates (brief highlight on changed items)

8. Check connection status:
   - Connection indicator should show green
   - Stop server, indicator should show red/yellow
   - Restart server, indicator should reconnect

9. Check sidebar:
   - Collapse sidebar with toggle button
   - Verify main content expands
   - Expand sidebar

10. Verify styling:
    - Dark theme, clean/modern appearance (Linear-inspired)
    - No visual glitches or overflow issues
  </how-to-verify>
  <resume-signal>Type "approved" if dashboard meets expectations, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
User has visually inspected and tested all dashboard features.
</verification>

<success_criteria>
- User approves dashboard functionality
- Or: User provides specific feedback that is addressed in a follow-up plan
</success_criteria>

<output>
After completion, create `.planning/phases/06-task-visibility/06-07-SUMMARY.md`
</output>
